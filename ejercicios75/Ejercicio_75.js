// Crea una función que reciba dos números como parámetros y devuelva el mayor de ellos.

function mayorNumero(num1, num2) {
    if (num1 > num2) {
        return num1;
    } else {
        return num2;
    }
}


let resultado = mayorNumero(10, 5);
console.log(resultado); 